[project]
name = "suryamon-analytics"
version = "0.1.0"
description = "IEC 61724-1 KPI engine for the Suryamon solar monitoring stack"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "pyyaml>=6.0",
]
